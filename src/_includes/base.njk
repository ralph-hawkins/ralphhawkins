<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} - {% endif %}My Site</title>
</head>
<body>
    <main>
        {% if title and layout != "home" %}
            <h1>{{ title }}</h1>
        {% endif %}

        {{ content | safe }}
    </main>

    {% include "footer.njk" %}
</body>
</html>
